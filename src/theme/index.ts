import { color } from "./fields/color";
import { shadow } from "./fields/shadow";
import { spacing } from "./fields/spacing";
import { typography } from "./fields/typography";

export const theme = {
  color,
  shadow,
  typography,
  spacing,
};
